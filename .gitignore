# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
.env

# OS
.DS_Store
Thumbs.db

# Databricks / IDE
.databricks/
.vscode/
*.bundle/
.databricks-login.json

# Claude Code local tooling (not project deliverables) — commands/ is tracked,
# it's a project deliverable (repo-shared slash commands), not local config
.claude/*
!.claude/commands/
.ai-dev-kit/
.mcp.json
.mcp.json.bak
.github/skills/

# Raw data artifacts (these live in the UC Volume, not in Git — see CONTRIBUTING.md)
data/raw/
*.ndjson

# dbt
src/dbt/target/
src/dbt/dbt_packages/
src/dbt/logs/
dbt_profiles/.user.yml
